# BYS — İşlem durumu · 25.08.2026

> Bu dosya "nerede kaldık" notudur. Yeni oturumda **önce bunu oku**,
> sonra `acfk-bys-basin-yonetim-sistemi.md` hafıza dosyasını.

## Özet

25.08.2026 tarihinde iki belge geldi (WeTransfer):
- `13-duzeltme-listesi.md` — 18 maddelik kod incelemesi (24.08, `08a63b1`)
- `revize.docx` — **Yusuf'un WhatsApp notları** (25.08, 16:21–16:25) + 2 ekran
  görüntüsü → özeti `yusuf-notlari-20260825.md`

**İkisi de TAMAMEN kapatıldı.** Aynı gün akşam duyuru videosu da eklendi;
artık kapsam dışı bırakılan hiçbir madde yok. Hepsi GitHub'da (`master`).

---

## Yusuf'un 6 isteği — HEPSİ BİTTİ

| # | İstek | Durum |
|---|---|---|
| 1 | Eksik evrak tamamlamada alanların **sürümlü** hâli (önceki/sonraki) | ✅ |
| 2 | Talepler **ayrı alanlarda** alınsın, başvuran o alanı düzeltebilsin | ✅ |
| 3 | **Listemizde olmayan** evrak/bilgi ad-hoc talep edilebilsin | ✅ |
| 4 | "ilk bilgiler · düzeltme talebi 01 · 02" + **neyi yeni ekledi** | ✅ |
| 5 | Akreditasyon süzgeci: üye türü, kurum, **durum yukarı**, tarih | ✅ |
| 6 | Görsel + video yükleme → **basın bültenine** uyarlansın | ✅ |

Bülten ekleri görsel/video/PDF alıyor; duyuruda da ayrı bir **Video** alanı var
(aşağıya bakın).

### Duyuru videosu — 25.08 akşamı eklendi

Sabah "bilerek kapsam dışı" bırakılmıştı (Yusuf "bültene adapte" demişti);
akşam istendi ve yapıldı.

- **Ayrı `video_yolu` sütunu.** Videoyu `gorsel_yolu`na doldurmak liste
  önizlemesini kırardı; artık duyuruda görsel ve video **aynı anda** olabilir.
- Yönetim formunda **Video** alanı: yalnızca `mp4` / `webm` (beyaz liste,
  `->image()` gibi joker yok), 60 MB tavan.
- Üye ve kurum panelinde `<video controls preload="metadata">` — sayfada 10
  duyuru varken hepsinin videosu peşinen inmesin.
- Dosya `icerik.dosya` rotasından **Range destekli** akıyor (ileri sarma çalışır,
  60 MB PHP belleğine yüklenmez).
- 🧹 Yetim dosya taraması artık **`icerik` diskini de** kapsıyor: yetim bir video
  60 MB tutuyor. Kayıtlı sayılan liste `withTrashed()` — yumuşak silinmiş içerik
  geri alınabilir, dosyası durmalı.

Testler: `bys-icerik-testi.mjs` 21/21 (video açılıyor mu · Range 206 · akredite
olmayan erişemiyor) · `bys-duyuru-video-testi.mjs` 7/7 (yönetim formundan gerçek
yükleme — 💣 Livewire sınırı aşan dosyayı sessizce reddediyor, zinciri yürüten
tek şey gerçek bir yükleme).

---

## 18 maddelik düzeltme listesi — HEPSİ BİTTİ

md.1 telefon göçü · md.2 XSS saflaştırma · md.3 SVG · md.4 kurum yetkilisi
çıkmazı · md.5 (Yusuf'un akışıyla değişti) · md.6 kullanıcı/rol ekranı ·
md.7 kuyruklar · md.8 CSV · md.9 bölge varsayılanı · md.10 red e-postası ·
md.11 anahtar şeması · md.12 kapı uyarıları · md.13 yetim dosya ·
md.14 panel listesi · md.15 `ureten_id` · md.16 evrak boyutu ·
md.17 indeksler · md.18 ölü kod

### Listede OLMAYAN, yolda bulunan 4 hata
1. 🚨 **Testler CANLI veritabanına bağlanıyordu** — `bootstrap/cache/config.php`
   `phpunit.xml`'i yok sayıyor. `RefreshDatabase` bir `migrate:fresh` atsaydı
   gerçek veri giderdi. Ayrı `bys_test` + `TestCase` emniyet kilidi kuruldu.
   **Veri kaybı OLMADI** (sayımla doğrulandı).
2. 💣 **Göç sırası bozuktu** — `basvuran_telefon` sütununu sonraki göç
   yaratıyor; sıfırdan kurulumda `migrate` hiç çalışmıyordu.
3. 💣 **Livewire 12 MB'ı sessizce reddediyordu** (ValCert'teki tuzağın aynısı).
4. 💥 **`Kurum::calisan_araligi` enum cast'i** — kurumsal başvuruda düzeltme
   sayfası ve inceleme ekranı **500** veriyordu. "İlk bilgiler" tüm alanları
   okumaya başlayınca çıktı.

---

## Sunucuda yapılan değişiklikler (kod dışı)

- `/etc/php/8.3/fpm/pool.d/bys.conf` → `upload_max_filesize 64M`,
  `post_max_size 80M`. Yedek: `/root/backups/bys.conf.bak-*`
- `/etc/nginx/sites-available/byd.ordolive.com` → `client_max_body_size 96M`.
  Yedek: `/root/backups/bys-nginx.bak-*`
- ⚠️ **30 sn kesinti yaşandı**: pool dosyasında değerin yanına `# yorum`
  yazmak php-fpm'i düşürüyor ve **o PHP'yi kullanan tüm siteler 502** veriyor.
  Düzeltildi. Bkz. hafıza: `php-fpm-satir-sonu-yorum.md`
- **Yeni veritabanı**: `bys_test` (PostgreSQL :5433, sahip `bys`) — yalnızca
  testler için. Geliştirme verisi `bys`de.
- Horizon yeniden başlatıldı; üç süpervizör aktif (kart/posta/default).

---

## Doğrulama (hepsi yeşil, 25.08 itibarıyla)

```bash
cd <uygulama-dizini>
sudo -u bys composer denetle      # pint + phpstan → 0 hata
sudo -u bys php artisan test      # 66 test

# Uçtan uca (⚠️ TEK TEK çalıştır: başvuru hız sınırı 10 dk'da 5,
#            her koşudan önce `php artisan cache:clear`)
node /root/bys-insan-senaryosu.mjs        # 51/51
node /root/bys-basvuru-akisi-testi.mjs    # 22/22
node /root/bys-bireysel-akis-testi.mjs    # 33/33
node /root/bys-icerik-testi.mjs           # 21/21 (duyuru videosu dâhil)
node /root/bys-duyuru-video-testi.mjs     # 7/7  (yönetim formundan yükleme)
node /root/bys-duzeltme-shot.mjs          # 14/14 + görüntü
node /root/bys-yonetim-shot.mjs           # 12 panel ekranı
```

🪤 **Üç ağır betiği arka arkaya çalıştırma** — hız sınırına takılır ve
"gerileme" gibi görünür. Aralarına `cache:clear` koy.

---

## AÇIK KALANLAR (bugünkü işten bağımsız, önceden de vardı)

- 🎯 **Kapıda gerçek deneme** (müşteriyle) — Aşama 07'nin tek kalan adımı
- ⚖️ **KVKK metinleri yok** — sayfalar hazır, içerik kulüpten gelecek.
  `docs/kvkk-taslak.md` hukuk onayı bekliyor
- 🔑 **ACFK kariyer panelinin parolası hâlâ değiştirilmedi** (adres çubuğuna
  düşmüştü, CF kayıtlarında da var) — BYS'den ayrı bir iş
- ⏳ Yönetim paneli için ayrı alt alan (`yonetim.<alanadi>`) canlıya
  çıkmadan ayrılacak (plan md.11)
- 💾 Yedekler aynı sunucuda; **dışarı kopyalama eksik** — hedef seçilmeli
  (BYS için ayrı bir R2 kovası mı, başka bir sunucu mu). Kimlik bilgisi
  gerektirdiği için karar müşteride/İbrahim'de
- 📌 Yusuf: "Başvurular başlasın, engel değil" demişti — teknik engel yok

## Karar bekleyen

- Kapı uyarılarında süre ayarları: `mukerrer_okutma_saniye`=30 (aynı kapı),
  `kart_paylasimi_saniye`=120 (tüm kapılar). Bu değerler benim önerim;
  müşteriyle teyit edilmeli.
